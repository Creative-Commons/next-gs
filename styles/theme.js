import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
    palette: {
        primary: {
            light: '#757ce8',
            main: '#ff0000',
            dark: '#002884',
            contrastText: '#fff',
        },
    }
    
});

export default theme;